from setuptools import setup

setup(name='coselection',
      version='0.1',
      description='Create co-selection networks in igraph, compute graph metrics over attributes and identify cores',
      url='http://github.com/gaussit/coselection',
      author='gaussit',
      license='MIT',
      packages=['coselection'],
      zip_safe=False)